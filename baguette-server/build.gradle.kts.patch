--- a/canvas-server/build.gradle.kts
+++ b/canvas-server/build.gradle.kts
@@ -36,7 +_,24 @@
         }
     }
 
-    activeFork = canvas
+     val baguette = forks.register("baguette") {
+         forks = canvas
+         upstream.patchRepo("paperServer") {
+             upstreamRepo = canvas.patchedRepo("paperServer")
+             patchesDir = rootDirectory.dir("baguette-server/paper-patches")
+             additionalAts?.set(rootDirectory.file("build-data/baguette-paperserver.at"))
+             outputDir = rootDirectory.dir("paper-server")
+        }
+        upstream.patchDir("canvasServer") {
+            upstreamPath = "canvas-server"
+            excludes = setOf("src/minecraft", "paper-patches", "minecraft-patches", "build.gradle.kts", "build.gradle.kts.patch")
+            patchesDir = rootDirectory.dir("baguette-server/canvas-patches")
+            additionalAts?.set(rootDirectory.file("build-data/baguette-canvasserver.at"))
+            outputDir = rootDirectory.dir("canvas-server")
+        }
+    }
+
+    activeFork = baguette
 
     spigot {
         enabled = true
@@ -124,21 +_,25 @@
         java {
             srcDir("../paper-server/src/main/java")
             srcDir("../paper-server/src/generated/java")
-            srcDir("src/generated/main/java")
+            srcDir("../canvas-server/src/generated/main/java")
+            srcDir("../canvas-server/src/main/java")
         }
         resources {
             srcDir("../paper-server/src/main/resources")
-            srcDir("src/generated/main/resources")
+            srcDir("../canvas-server/src/generated/main/resources")
+            srcDir("../canvas-server/src/main/resources")
         }
     }
     test {
         java {
             srcDir("../paper-server/src/test/java")
-            srcDir("src/generated/test/java")
+            srcDir("../canvas-server/src/generated/test/java")
+            srcDir("../canvas-server/src/test/java")
         }
         resources {
             srcDir("../paper-server/src/test/resources")
-            srcDir("src/generated/test/resources")
+            srcDir("../canvas-server/src/generated/test/resources")
+            srcDir("../canvas-server/src/test/resources")
         }
     }
 }
@@ -167,7 +_,7 @@
 }
 
 dependencies {
-    implementation(projects.canvasApi)
+    implementation(projects.baguetteApi)
     implementation("ca.spottedleaf:concurrentutil:0.0.3")
     implementation("org.jline:jline-terminal-ffm:3.27.1") // use ffm on java 22+
     implementation("org.jline:jline-terminal-jni:3.27.1") // fall back to jni on java 21
@@ -238,14 +_,14 @@
         val gitBranch = git.exec(providers, "rev-parse", "--abbrev-ref", "HEAD").get().trim()
         attributes(
             "Main-Class" to "org.bukkit.craftbukkit.Main",
-            "Implementation-Title" to "Canvas",
+            "Implementation-Title" to "Baguette",
             "Implementation-Version" to implementationVersion,
             "Implementation-Vendor" to date,
-            "Specification-Title" to "Canvas",
+            "Specification-Title" to "Baguette",
             "Specification-Version" to project.version,
-            "Specification-Vendor" to "Canvas Team",
-            "Brand-Id" to "canvasmc:canvas",
-            "Brand-Name" to "Canvas",
+            "Specification-Vendor" to "Baguette Team",
+            "Brand-Id" to "canvasmc:baguette",
+            "Brand-Name" to "Baguette",
             "Build-Number" to (build ?: ""),
             "Build-Time" to buildTime.toString(),
             "Git-Branch" to gitBranch,
@@ -304,7 +_,7 @@
     jvmArgumentProviders.add(provider)
 }
 
-val generatedDir: java.nio.file.Path = layout.projectDirectory.dir("src/generated/main/java").asFile.toPath()
+val generatedDir: java.nio.file.Path = layout.projectDirectory.dir("../canvas-server/src/generated/main/java").asFile.toPath()
 val paperGeneratedDir: java.nio.file.Path = layout.projectDirectory.dir("../paper-server/src/generated/java").asFile.toPath()
 idea {
     module {
@@ -349,8 +_,8 @@
     })
     jvmArgs("--add-modules=jdk.incubator.vector")
 
-    if (rootProject.childProjects["canvas-test-plugin"] != null) {
-        val testPluginJar = rootProject.project(":canvas-test-plugin").tasks.jar.flatMap { it.archiveFile }
+    if (rootProject.childProjects["test-plugin"] != null) {
+        val testPluginJar = rootProject.project(":test-plugin").tasks.jar.flatMap { it.archiveFile }
         inputs.file(testPluginJar)
         args("-add-plugin=${testPluginJar.get().asFile.absolutePath}")
     }
